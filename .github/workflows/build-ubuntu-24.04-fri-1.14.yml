name: ubuntu-24.04-fri-1.14
on:
  pull_request:
    branches:
      - jazzy
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * 0"

jobs:
  build:
    uses: ./.github/workflows/build-ubuntu-24.04.yml
    with:
      fri_version: 1.14
