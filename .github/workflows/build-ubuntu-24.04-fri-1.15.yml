name: ubuntu-24.04-fri-1.15
on:
  pull_request:
    branches:
      - jazzy
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * 0"

jobs:
  build:
    uses: ./.github/workflows/build-ubuntu-24.04.yml
    with:
      fri_version: 1.15
